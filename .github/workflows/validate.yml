name: validate

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: validate data contract
        run: python scripts/validate_data_v3.py
      - name: validate source policy
        run: python scripts/validate_sources.py
      - name: check JavaScript syntax
        run: node --check app.js && node --check enhance-v2.js && node --check lib/time.js
      - name: test time semantics
        run: node scripts/test_time.mjs
